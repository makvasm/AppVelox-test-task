import styled from 'styled-components';
import variables from './Variables';

const Button = styled.button`
  background-color: ${variables.colorAdditional};
  padding: 10px 15px;
  border-radius: ${variables.borderRadius};
  color: #FFFFFF;
`;

export default Button;